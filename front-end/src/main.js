import { createApp } from 'vue'
import App from './App.vue'  // Importa o componente principal

// Cria e monta a aplicação Vue
const app = createApp(App)
app.mount('#app')  // Monta no elemento com id "app" do index.html